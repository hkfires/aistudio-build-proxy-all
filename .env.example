# Copy this file to `.env` and replace placeholder values with your own.
# Only commit `.env.example`; keep your `.env` file local.

# API key used by the Go proxy to authenticate inbound requests.
AUTH_API_KEY=your_set_api_key_here

# Global Camoufox browser configuration. Headless can be true, false, or virtual.
CAMOUFOX_HEADLESS=virtual
# Optional proxy shared by all instances, e.g. http://user:pass@host:port
CAMOUFOX_PROXY=

# Define how many browser instances to launch, then provide per-instance overrides.
CAMOUFOX_INSTANCE_COUNT=1

# Instance 1 configuration (add more blocks by incrementing the index).
CAMOUFOX_INSTANCE_1_COOKIE_FILE=user1_cookie.json
CAMOUFOX_INSTANCE_1_URL=https://aistudio.google.com/apps/drive/1YL729RgvbQc4Zw3z47gghil0abBqg3Rh
# CAMOUFOX_INSTANCE_1_HEADLESS=false
# CAMOUFOX_INSTANCE_1_PROXY=http://user:pass@specific-proxy.com:9999
